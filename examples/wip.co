// this file is used for development and may not make sense or even compile


fun multiply<T>(a T, b int) T
  a * T(b)

fun main(x int)
  multiply(1, x)

  // call add in wip2.co (compile with both source files)
  add(x, 2)
