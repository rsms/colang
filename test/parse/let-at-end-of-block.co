fun a() {
  b()
  x = 4
}
fun b() {
  x int
}
fun c() -> x = 4
fun d() -> var x int
# fun d() -> x int  # error since x is in rvalue position

#*!AST
(Fun a nil (body
  (Block
    (Call (Id b) nil)
    (Let x (IntLit 4)))))
(Fun b nil (body (Let x)))
(Fun c auto (body (Let x (IntLit 4))))
(Fun d auto (body (Let x)))

*#
